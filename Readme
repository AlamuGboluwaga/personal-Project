Buid a CRUD rest API with typescript + Express.js Sequelize + Sqlite3

improve performance by using express-validator

[ ] set up typescript for dev
[ ] basic express server with typescript
[ ] setup database 
[ ] create model
[ ] create todo
[ ] read todo with pagaination 
[ ] raed todo with id
[ ] update todo with id
delete todo


1 npm init -y

2 yarn add -D typescript ts-node-dev
create scripts in package.josn { "scripts":"ts-node-dev --respawn src/server.ts"}

3 npx tsc -init
in package.json create { "build": "tsc",
    "start": "node dist/src/server.js"} then do yarn  build
    "compile":"tsc -w"

     yarn add -D npm install --save @types/express
     yarn add -D @types/uuid 
     loadash (npm install --save @types/lodash.chunk or yarn add @types/lodash.chunk)
    import _ from "lodash";
    export = chunk; 
    how to use _.pick(user,['name','email','lastmane'])

    Joi  complexity
    # yarn add joi-password-complexity not need it has been replaced by bcrypt
    npm i bcrypt @types/bcrypt
   yarn add @types/bcrypt
   npm i jsonwebtoken @types/jsonwebt
    yarn add config @types/config
   npm install --save @types/config
node
Welcome to Node.js v16.16.0.
Type ".help" for more information.
> require('crypto').randomBytes(64).toString('hex')
